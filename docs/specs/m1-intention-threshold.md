# Disposable M1 intention threshold

Questions: does broad solid shading with sparse surface glyphs absorb imperfect generated detail without hiding navigation cues (#14)? Does an intention visibly shaping the next threshold feel causal rather than a destination menu?

Extend the isolated M1 prototype branch and page. Preserve all earlier treatments and exact source/derived asset bytes. Style comparison mode retains identical mesh, placements, camera, floor and collision. Compare faceted control, broad solid control and broad solid plus sparse glyphs. A deterministic material corruption patch on existing surface triangles supplies a labeled ugly/incomplete-detail stress case; no collider edits or geometry removal to hide failure.

A separate threshold mode adds one authored opening and one composed destination to the disposable scene. Before it, the player can press E and sustain a look gesture while looking upward (Ascend) or level/downward (Seek). Gesture interpretation only activates within the threshold's approach region. A short contextual cue describes the gesture; no destination cards, prompt input or gameplay system. Ascend means vertical emphasis/open upper volume, not stairs promised by a flat-floor walker. Seek means focal emphasis/revealing details. These semantic constraints are input to a deterministic fixture composer, not named chunk destinations. Both reshape the same spatial shell; collision remains fixed across intentions. No new GPU job or boundary-conditioning claim.

Prototype world state explicitly stores world id, chosen intention, semantic constraints, threshold phase, seed, resulting composition and causal events. A dedicated prototype-only sessionStorage key preserves this scratch world across reload in the same tab; no M0 world/player/storage/API writes. Commit intention and composition before opening the physical seam. Once crossed, the accepted result stays stable on return; a labeled developer reset clears only this experiment. Reload while recording/building must not leave an impassable half-state.

Acceptance: explore the generated approach; reach the closed seam; out-of-range gesture cannot select; hold gesture records intention; visible fixture composition forms behind the same opening; seam opens only when ready; cross by actual collision movement and return; record phase and position; reload preserves accepted composition. Test both semantic outcomes at the same camera and compare no-intention baseline. Assess emotion as an observational hypothesis, not a user-study result.

Shared implementation seam: lead owns page/main, threshold-state.ts and tests, root configuration, docs. Visual subagent owns only apps/web/src/m1-prototype/treatments.ts: buildSolidGlyph(app,data,placements) returns {root, setMode('solid'|'hybrid'), setStress(boolean), update(cameraPosition), stats}; root children are one group per asset placement. data has mesh/transform/glyphs compatible with existing scene.json. Subagent must not change original source/derived files, root config, main renderer or collision. It may export reusable stress classifier for faceted control. Integration review will verify rendering counts and invariance.
