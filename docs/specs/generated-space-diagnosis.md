# Frozen-space rejection diagnosis

Question: how many of the prior31 unresolved cases are demonstrated interpreter false negatives? Use only the frozen36 artifacts; no GPU jobs, geometry repair, gameplay, styling or model changes. Keep all historical files immutable. Runtime branch codex/generated-space-diagnosis; integration receives documentation/evidence only. Base integration d462b3c, previous runtime7c87e25.

First freeze observational instrumentation of the corrected interpretation endpoint. Replay all36 at their already-locked selected scales with expanded→legacy same-scale fallback. Produce source/hash-bound ordered traces for modifier compilation, support sampling, every considered and omitted entry proposal, projection/body screening, discovered components/edges, candidate route failure, witness predicates, budget exhaustion and final seam movement. Log measured values, thresholds, original triangle IDs and stable proposal/node/route identifiers where available. Distinguish first failing predicate from unexecuted predicates; do not invent observations. Traces may be compressed but not silently truncated. Prove verdict, route, witness and diagnostic parity against prior corrected attempts, ignoring timing/provenance only. Original navigator and physical dimensions stay unchanged.

Only after traces identify systematic false negatives, specify a targeted correction with synthetic positive/negative tests. Priorities: full-projection entry exclusion, proposal/route coverage, modifier scope, topology witnesses. Each correction is separately frozen by file/content hash before any36-candidate evaluation. Preserve baseline and every changed phase; report newly recovered and lost cases against the exact predecessor. Keep the selected scale fixed. No candidate IDs/categories/known labels in algorithms. A retrospective correction is outcome-informed, not blind generalization.

A valid entry must be physically reachable from outside the source bounds without crossing solid geometry or creating authored destination support. Any more permissive entry rule must prove this invariant with original triangles, preserve sealed-internal-hole rejection, and demonstrate final reversible seam traversal. Search coverage changes must have explicit bounded work and retain previous successful proposal paths. Scope correction must preserve intended local constraints rather than deleting them; witnesses remain quantitative, not unconditional acceptance.

Browser-verify every recovered selected-scale case through original support, >=3m generated route, return and persistence. First attempt is primary; repeats are separate. Previously proven scale misses (large_open_interior-1-7, corridor-1-7) and the three prior accepted cases are outside the31-unresolved denominator. New failures remain unresolved unless a concrete geometry defect or interpreter false negative is established. Final report includes per-candidate rejection trace pointers, phase comparisons, category totals, trace coverage, browser records and remaining uncertainty. Do not decide to change models based on censored bounded-search failures.

Pinned seams: scripts/diagnosis/trace.ts exports TraceEvent {stage,kind,...observations} and TraceSink(event). routes-traced.ts exports assessDiagnosedSpace with the old assessInterpretedSpace input plus optional trace:TraceSink, same assessment return shape. criteria-traced.ts exports compileDiagnosedCriteria(rawIntent,semantics?,trace?) returning byte-equivalent corrected SpatialCriteria. These modules are prototype-local; existing frozen modules and production protocol/config are unchanged.
