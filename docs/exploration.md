# Pinned integration seams

Read packages/protocol/src/index.ts as exact shared TS contract. Integration branch codex/integration. Lead alone changes root configuration, protocol, world-model and scripts/dev.mjs.

## Coordinates and fixtures
Metres, Y up. Arrival room id `arrival`, position [0,0,0], bounds local [-7,0,-9]..[7,4,9]. Observatory id `observatory`, position [0,0,-18], same local bounds. Arrival north portal at WORLD [0,0,-9] links observatory; observatory south same WORLD point links arrival. Portal width 3.2 height 3.2. Eye starts [0,1.65,5], yaw 0 pitch 0, looks -Z. Physical radius .3. Flat authored collider boxes. Exclude floor/ceiling from horizontal blocking (only boxes crossing player's body y=.2..1.8). Closed north seam until observatory ready. Outer boundaries solid. Interior collider boxes extracted from actual GLB meshInstance AABBs, transformed into world space. No GPU dependency for fixtures.

## Artifact writer / server contract
`scripts/create-fixtures.ts` writes `artifacts/fixtures/arrival/scene.ply`, `collider.glb` and observatory equivalents. Script deterministic and mkdir recursive. Each collision GLB contains unit-cube mesh nodes with scale/translation; renderer may derive world AABBs from loaded render mesh instances. Visual PLY binary little endian, authored rooms with equipment, windows, lights and distinct palettes. Worker fake copies these files. Runtime data ignored by Git and reproducible.

Orchestrator `createOrchestrator({dataDir?, artifactDir?})` async returns `{server, close}`; server is node http.Server unbound so test listen(0). Main binds 127.0.0.1:4310 unless HOST/PORT. `GET /api/health`; `GET /api/world` returns World, lazily initialize stable singleton fixture world. `GET /api/chunks/:id` returns Chunk after optional ~350ms fixture delay (initial world metadata includes both; browser requests second on approach). `PUT /api/player` JSON PlayerPose → saved pose response. `GET /artifacts/:hash/:filename` serves immutable imported bytes; backend computes SHA256 and copies fixtures to content-addressed artifact storage on initialization; filenames scene.ply/collider.glb. SQLite at dataDir/world.sqlite. Restart uses original DB records and artifacts, no regeneration. Root artifacts directory defaults path.resolve('artifacts'), data .runtime. Backend owns all services/orchestrator/** plus its tests. Browser uses same-origin relative /api and /artifacts; Vite proxy to :4310.

## World movement function
`movePlayer(position: Vec3, displacement: Vec3, boxes: CollisionBox[], radius=0.3): Vec3` in packages/world-model/src/index.ts. Axis-separated XZ sweep/substeps, fixed eye Y. `chunkAt(position: Vec3, chunks: Chunk[]): string | null`; `shouldLoadNeighbor(position: Vec3, portal: Portal, distance=6): boolean`. Browser owns request/retention/load states and reports actual player pose with periodic PUT.

## Worker
Python stdlib HTTP service (no install needed fake mode), port 4320 loopback by default. GET /health /capabilities; POST /jobs JSON WorkerJobRequest; GET /jobs/:id, POST /jobs/:id/cancel, GET /jobs/:id/artifacts. Job status WorkerJob. Artifacts manifests use project Artifact array, local HTTP artifact routes, backend fake-fixture-v1. Heavy concurrency exactly 1. Explicit bind/token config for remote use, documented SSH tunnel preferred. No browser contact. Persistent fake job state preferred. GPU probe read-only reports nvidia-smi/OS/Git/Python and records unavailable values honestly.

## Resolved grilling questions
The brief chooses renderer, explicit geometry, local SQLite, fake-before-real worker, hardware separation and workflow. Remaining M0 choices are reversible and covered by autonomous authorization: two flat rooms, capsule approximation against authored GLB boxes, fixtures honestly labeled, single local player. Loss/restart and failed stream scenarios gate acceptance. Test seams are HTTP restart behavior, movement API and browser user flow. Real GPU access is a separate blocker, not an architectural choice.

Pose writes may include `X-Pose-Time` (epoch milliseconds, browser performance.timeOrigin + performance.now). The Mac and browser share one clock. SQLite atomically persists the newest timestamp with the pose and ignores older arrivals, returning the accepted pose. Missing timestamps use server time. This prevents periodic/keepalive requests arriving out of order from replacing the exit pose. This is single-local-player ordering, not multi-device clock synchronization.

# GPU readiness seams (2026-09-14)

Current spec: docs/specs/gpu-worker-readiness.md. Lead owns packages/protocol, services/orchestrator, root config/CI and benchmark integration. Worker agent owns services/gpu-worker (except later benchmark candidate runner, coordinated). Bootstrap agent owns scripts/bootstrap-worker.sh, scripts/deploy-worker.sh, scripts/tests and docs/deployment.md. Research owns docs/research/first-backend-benchmark.md only.

`CapabilityReportSchema` and `WorkerCapabilitiesSchema` in protocol are exact JSON contracts. Hardware report: schemaVersion 1, capturedAt ISO, host {hostname,os,release,arch}, python {version,executable}, gpu {status,devices:[{index,uuid,name,driverVersion,vramTotalMiB,vramFreeMiB}],error}, cuda {driverStatus,driverSupportedVersion,toolkitStatus,toolkitVersion}, pytorch {status,version,cudaBuildVersion,cudaAvailable,error}, nvidiaExecution:'not_run', notes:string[]. Probe status available/unavailable/failed/not_checked; unknown measurements null, not zero. Default no torch import; opt-in `--check-torch` may inspect availability/version in a bounded subprocess, not run inference. /capabilities caches a report for process lifetime (startup capture) to avoid repeatedly blocking job requests.

Worker capabilities version1: {schemaVersion:1,backend:{id,version,mode:'fixture'|'command',requiresGpu},maxConcurrency:1,formats:['ply','glb'],cancellation:true,hardware:CapabilityReport,version:{service,version,python,gitCommit}}. GET /health and /version remain. Job/manifest backend strings identify selected adapter. WorkerJobRequest.fixture becomes optional globally; fixture adapter requires arrival/observatory; command receives the full validated request. Original request ID/job state API retained.

Worker config: `--backend fixture|command` / WORKER_BACKEND default fixture. Command adapter argv comes from WORKER_COMMAND_JSON (JSON string array), optionally `--command-json`; trusted local configuration only, shell=False. Appends `--request <absolute request.json> --output <absolute staging directory>`. Worker deadline `--job-timeout` / WORKER_JOB_TIMEOUT seconds default 900. Command must produce scene.ply and collider.glb. No model executable/image path is selected by HTTP requests. Adapter interface can be implemented in Python too; HTTP lifecycle/publishing/hashing remain worker-owned. Fixture id stays fake-fixture-v1; generic command adapter id command-v1, version1; logs/benchmark records retain actual model commit separately. Require valid nonempty artifact formats before successful publication (PLY header binary_little_endian, GLB magic/version/declared length), no fabricated collision proxy.

Bootstrap CLI: scripts/bootstrap-worker.sh --repo <Git URL/path/bundle> --ref <full40hex commit> --prefix <absolute path> [--python python3]. Linux only normal execution, no sudo/apt/GPU/model installs. Creates prefix/releases/<commit>/repo and venv, prefix/shared/{data,fixtures,config}, prefix/current symlink, prefix/run-worker script. run-worker uses selected release venv and services/gpu-worker/worker.py with loopback host port4320, shared dirs, WORKER_BACKEND env. Preserve operator config prefix/shared/config/worker.env on rerun; no secrets in logs. Refuse dirty release code; exact commit checkout. Deploy helper if provided packages Git commit bundle (plus fixture assets optional) via SSH with BatchMode/StrictHostKeyChecking=yes and shell-safe args. Never presume Windows shell is Linux; require SSH landing in Linux/WSL. Docs exact Mac→Linux commands and current unverified host candidate.

Mac API additions: configure createOrchestrator({workerUrl?,workerToken?}) and CLI env WORKER_URL / WORKER_TOKEN; existing world unaffected. GET /api/worker/capabilities; POST /api/worker/jobs; GET /api/worker/jobs/:id; POST /api/worker/jobs/:id/cancel; POST /api/worker/jobs/:id/import (poll success first, verifies/downloads artifacts and saves accepted job manifest in SQLite); GET /api/worker/jobs/:id/artifacts returns saved local Artifact[] even when worker offline. Imported artifact routes share /artifacts/:hash/:filename. Worker artifact URLs must be relative path on configured worker origin, no redirects, size bounds and timeouts, hashes checked before manifest commit. Store request prompt/seed/boundary with imported record for provenance. This prepares real backend integration without mutating accepted M0 topology.

# M1 isolated experiment

Contract: docs/specs/m1-generated-chunk-experiment.md. Creative hypotheses: docs/creative-direction.md. The disposable page is `m1-prototype.html`, with `?variant=faceted|graphic|glyph|original`. Preparation writes ignored `apps/web/m1-prototype-public/m1-prototype/{scene.json,scene.ply}` from verified benchmark artifacts. No changes to M0 world/player APIs or shared protocol. Generated mesh coordinates and collision derivation are pinned in the M1 spec. All variants share transforms, scene layout and collision. Authored support floor/boundary are labeled; no automatic room-generation claim.

# M1 solid/glyph and intention continuation

Experiment contract: docs/specs/m1-intention-threshold.md. Runtime stays on codex/m1-intention-prototype; evidence/verdict: docs/research/m1-intention-threshold.md. Style variants add solid/hybrid plus deterministic material stress without editing source/derived mesh or collider. Threshold mode is selected by ?experiment=intention. It composes one authored destination from the existing generated asset, while the original comparison mode remains fixed.

Prototype-only state lives under sessionStorage key vastness:PROTOTYPE-wipe-me:intention-v1; never M0 APIs/storage. World threshold phases unshaped → forming → ready → crossed; event sequence records intention-recorded, composition-ready, crossed and returned. Semantic constraints {verticalEmphasis,focalEmphasis,upperOpenness,detailRevelation,continuousFloor:true} feed a controlled-fixture-v1 composer with seed 14; no intention name/destination id crosses that composer seam. Source and destination geometry/collision are fixed across Ascend/Seek. Intention persists across same-tab reload, pose resets; closing the tab is outside this scratch persistence contract. E starts a sustained look gesture near the closed seam; contact position is valid. No real generation or permanent gameplay/art decision claimed.

# Free-form intention generation experiment

Contract: `docs/specs/intention-generated-destination.md`. Evidence and bounded verdict: `docs/research/intention-generated-destination.md`. Runtime remains isolated on `codex/intention-generation-prototype`; integration receives documentation and evidence only.

The new Mac API on 4311, browser on 5175 and worker on 4321 (Mac SSH forward 14321) leave M0/M1 storage, pages and accepted artifacts unchanged. `SemanticIntent` and `WorldRecord` are prototype-local; shared `WorkerJobRequest` still transports the typed JSON envelope in `prompt`. Exact API and retry seams are in the isolated spec; no production protocol migration.

Worker commit `04986defd1de38609cfb69c8d6c78ae4704e5670` ran all three final comparisons with real SDXL-Turbo then TRELLIS on the RTX 3090. Each destination was crossed into, returned from and reused after restart. This establishes intent-generated artifacts in traversable authored support space; generated navigable interiors remain unproven.

# Generated passage experiment (2026-09-16)

The next isolated experiment proves short generated-supported interiors: `docs/specs/generated-passage.md`, measured report `docs/research/generated-passage.md`. Runtime stays on `codex/generated-passage-prototype`; integration receives docs/evidence only. Existing intention pipeline and all M0/M1 source remain unchanged. Three new SDXL/TRELLIS jobs used worker04986def; tunnel and courtyard pass generated-floor entry/traversal/return/restart, arch fails. No fallback models downloaded.

Prototype-local `createSurfaceNavigator(mesh, transform, options?, approach?)` uses original GLB triangles for support and swept upright-body collision. `PassageAssessment` records sourceSha256, transform, options, route, per-sample original support IDs, sustained enclosure, exterior seam/approach and verdict. Authored approach contacts are -1; all accepted interior route contacts are nonnegative source triangle IDs. Uniform scales6/10/12, radius.3, height1.8, eye1.65, maxStep.25, maxSlopeDegrees35. The prior M1 voxel boxes and M0 movePlayer remain untouched.

Separate Mac service4312: `GET /api/passage/cases`, `GET /api/passage/cases/:id`, hash-qualified `GET /api/passage/cases/:id/assets/:kind/:sha/:filename`, `POST /api/passage/cases/:id/visits` with `{identity,eventId,event:'crossed'|'returned',position}`. Immutable case identity binds world/intent/semantics and all artifact/assessment hashes; retries are idempotent. Data `.runtime/generated-passage/inspection`, browser5176 `generated-passage.html`. No new intent input or production shared-protocol fields. `projectCoarseColors` transfers generated SH-DC cues to unchanged coarse meshes with the same chroma-aware algorithm and up to12 colors per artifact; does not infer emission/material physics.
