## Parent
#7

## What to execute
Run the first pinned original TRELLIS image-to-3D benchmark on the real NVIDIA appliance after #6 provides a verified Linux SSH endpoint. Mac preparation is implemented in #12; no NVIDIA execution or performance is claimed yet.

Follow `docs/deployment.md` to deploy a reviewed full Vastness Git SHA, verify the live worker SHA through the SSH tunnel, and inventory GPU model/UUID, driver, total/free VRAM, toolkit and PyTorch. Follow `docs/benchmark.md` for the exact opt-in Linux setup, preflight, standalone execution and `WORKER_COMMAND_JSON` configuration. Do not silently change dependency pins if the actual hardware rejects the proposed stack; retain a failed setup record and make the next experiment explicit.

Pinned case: microsoft/TRELLIS source `442aa1e1afb9014e80681d3bf604e8d728a86ee7`; microsoft/TRELLIS-image-large weights `25e0d31ffbebe4b5a97464dd851910efc3002d96`; DINOv2 source `7b187bd4df8efce2cbcbbb67bd01532c19bf4c9c`; official `T.png` SHA256 `e29ddc83a5bd3a05fe9b34732169bc4ea7131f7c36527fdc5f626a90a73076d2`; seed 1. Canonical executable pins are in `services/gpu-worker/benchmarks/pins.json`.

Request through the configured Mac API: `POST /api/worker/jobs` with `{"id":"trellis-smoke-1","prompt":"","seed":1}`. The worker must be configured in command mode with the prepared runner's absolute argv from `docs/benchmark.md`. Poll the Mac job route; after success use `POST /api/worker/jobs/trellis-smoke-1/import` and verify both local artifact hashes. Use a new ID/output directory per attempt.

## Acceptance criteria
- [ ] Verified Linux endpoint and deployed/live source SHA recorded; GPU/driver/toolkit/PyTorch inventory saved.
- [ ] Setup logs, installed package/environment versions, source and weight hashes, and licenses retained.
- [ ] One genuine CUDA generation completes, with full inference log, stage timings, Torch peak allocated/reserved bytes, and sampled device-memory evidence. Download/setup duration is reported separately.
- [ ] Genuine nonempty Gaussian PLY and mesh GLB pass export validation, the worker API and Mac hash/size import checks.
- [ ] Multi-view inspection checks matching splat/mesh coordinates and records visual quality, scale and collision limitations. A raw asset proxy is not claimed to be a walkable room.
- [ ] Actual findings, failures/OOMs and a measured next-experiment decision are written under `docs/research/`. Failure evidence is useful but does not satisfy successful-backend acceptance.
- [ ] No fabricated GPU figures, unsupported text/boundary-conditioning claims, or changes to the accepted M0 world.

## Blocked by
#6 and #12. The remaining execution depends on the actual NVIDIA PC becoming reachable; all Mac-side preparation can be completed independently.
