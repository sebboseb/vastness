## Parent
#1

## What to build
Serve real content-addressed fixture artifacts and world/chunk metadata via HTTP, persist pose/topology in SQLite and restore across restart. Verify using HTTP requests.

## Acceptance criteria
- [x] Behavior is demonstrated through the public interface.
- [x] Focused tests and relevant type/build checks pass.
- [x] Review completed and changes committed.

## Blocked by
None (can start immediately).

Completed in 426a216; GitHub #3 closed after lead verification.
