## Parent
#1

## What to build
Serve real content-addressed fixture artifacts and world/chunk metadata via HTTP, persist pose/topology in SQLite and restore across restart. Verify using HTTP requests.

## Acceptance criteria
- [ ] Behavior is demonstrated through the public interface.
- [ ] Focused tests and relevant type/build checks pass.
- [ ] Review completed and changes committed.

## Blocked by
None (can start immediately).
